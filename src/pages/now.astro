---
import Layout         from "../components/Layout.astro";
import PageLayout     from "../components/PageLayout.astro";
import * as nowPage from '../content/now.md';

const page_content  = await nowPage.compiledContent();
const postUrl       = new URL(`/about`, Astro.site).href;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": nowPage.frontmatter.title,
  "description": nowPage.frontmatter.description,
  "datePublished": (new Date()).toISOString(),
  "url": postUrl,
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": postUrl,
  },
  "author": {
    "@type": "Organisation",
    "name": "Asindu Drileba",
    "url": "https://asindu.xyz",
  },
  "publisher": {
    "@type": "Organisation",
    "name": "Asindu Drileba",
    "url": "https://asindu.xyz",
  },
};
---

<Layout title={nowPage.frontmatter.title} description={nowPage.frontmatter.description} ogType="article">
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  <PageLayout title={nowPage.frontmatter.title} date={(new Date())} description={nowPage.frontmatter.description}>
    <div set:html={page_content} />
  </PageLayout>
</Layout>
