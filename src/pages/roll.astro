---
import Layout         from "../components/Layout.astro";
import PageLayout     from "../components/PageLayout.astro";
import * as rollPage from '../content/roll.md';

const page_content  = await rollPage.compiledContent();
const postUrl       = new URL(`/roll`, Astro.site).href;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": rollPage.frontmatter.title,
  "description": rollPage.frontmatter.description,
  "datePublished": (new Date()).toISOString(),
  "url": postUrl,
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": postUrl,
  },
  "author": {
    "@type": "Organisation",
    "name": "Asindu Drileba",
    "url": "https://asindu.xyz",
  },
  "publisher": {
    "@type": "Organisation",
    "name": "Asindu Drileba",
    "url": "https://asindu.xyz",
  },
};
---

<Layout title={rollPage.frontmatter.title} description={rollPage.frontmatter.description} ogType="article">
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  <PageLayout title={rollPage.frontmatter.title} date={(new Date())} description={rollPage.frontmatter.description}>
    <div set:html={page_content} />
  </PageLayout>
</Layout>
